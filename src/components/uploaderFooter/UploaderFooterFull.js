const uploaderTheme = require("../../templates/theme.json");

function UploaderFooterFull() {

    return (
        <div className="footer" style={{backgroundColor: uploaderTheme.footer.default.background}}>
            
        </div>
    );

}

export default UploaderFooterFull;
